@import url('https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800&family=Dancing+Script:wght@700&display=swap');
:root{--verde:#009C3B;--azul:#002776;--amarelo:#FFDF00;--bg:#F0F0EC;--r:12px;--r2:8px;--shadow:0 4px 20px rgba(0,39,118,.10)}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
body{font-family:'Nunito',sans-serif;background:var(--bg);color:#111}
button{cursor:pointer;border:none;background:none;font-family:inherit;outline:none}
input,textarea,select{font-family:'Nunito',sans-serif}

/* LOGIN */
#login-page{min-height:100vh;display:flex;align-items:center;justify-content:center;background:linear-gradient(145deg,var(--azul),#003a1a);padding:20px}
.login-box{background:#fff;border-radius:var(--r);padding:44px 36px;width:100%;max-width:380px;box-shadow:0 20px 60px rgba(0,0,0,.3);text-align:center}
.login-logo{font-family:'Dancing Script',cursive;font-size:1.9rem;color:var(--azul);margin-bottom:4px}
.login-sub{font-size:.82rem;color:#888;margin-bottom:28px}
.fg{margin-bottom:14px;text-align:left}
.fg label{display:block;font-weight:700;font-size:.82rem;color:var(--azul);margin-bottom:5px}
.fg input{width:100%;padding:10px 13px;border:2px solid #ddd;border-radius:var(--r2);font-size:.92rem;outline:none;transition:.2s}
.fg input:focus{border-color:var(--verde)}
.btn-login{width:100%;padding:13px;background:var(--azul);color:#fff;border-radius:var(--r2);font-size:.95rem;font-weight:800;margin-top:6px;transition:.2s}
.btn-login:hover{background:#1a3a8a}
.login-err{color:#c0392b;font-size:.82rem;margin-top:10px;display:none}
.back-link{margin-top:18px;font-size:.8rem;color:#aaa;display:block}
.back-link a{color:var(--verde);font-weight:700}

/* LAYOUT */
#admin-page{display:flex;min-height:100vh}
.sidebar{width:230px;background:var(--azul);color:#fff;display:flex;flex-direction:column;flex-shrink:0;position:sticky;top:0;height:100vh;overflow-y:auto}
.sb-brand{padding:22px 18px;font-family:'Dancing Script',cursive;font-size:1.4rem;color:var(--amarelo);border-bottom:1px solid rgba(255,255,255,.1)}
.sb-brand small{display:block;font-family:'Nunito',sans-serif;font-size:.65rem;color:rgba(255,255,255,.5);letter-spacing:2px}
.sb-nav{flex:1}
.sb-nav ul{list-style:none}
.sb-nav a{display:flex;align-items:center;gap:9px;padding:13px 18px;color:rgba(255,255,255,.7);font-weight:700;font-size:.88rem;transition:.2s;border-left:3px solid transparent;cursor:pointer}
.sb-nav a:hover,.sb-nav a.active{color:#fff;background:rgba(255,255,255,.08);border-left-color:var(--amarelo)}
.sb-footer{padding:18px;border-top:1px solid rgba(255,255,255,.1)}
.btn-logout{width:100%;padding:10px;background:rgba(255,255,255,.08);color:rgba(255,255,255,.65);border-radius:var(--r2);font-weight:700;font-size:.85rem;transition:.2s}
.btn-logout:hover{background:rgba(192,57,43,.4);color:#fff}
.btn-viewsite{display:flex;align-items:center;gap:6px;color:rgba(255,255,255,.55);font-size:.82rem;font-weight:700;margin-bottom:10px;cursor:pointer}

/* CONTENT */
.admin-content{flex:1;padding:28px;overflow-x:hidden}
.panel{display:none}.panel.active{display:block}
.panel-title{font-size:1.55rem;font-weight:900;color:var(--azul);margin-bottom:24px;padding-bottom:10px;border-bottom:3px solid var(--amarelo);display:flex;align-items:center;gap:10px}
.card{background:#fff;border-radius:var(--r);padding:22px;box-shadow:var(--shadow);margin-bottom:22px}
.card h4{font-weight:800;color:var(--azul);margin-bottom:16px;font-size:.92rem;display:flex;align-items:center;gap:8px}

/* FORMS */
.form-row{display:grid;grid-template-columns:1fr 1fr;gap:14px}
.fg-admin{margin-bottom:14px}
.fg-admin label{display:block;font-weight:700;font-size:.82rem;color:var(--azul);margin-bottom:5px}
.fg-admin input,.fg-admin textarea,.fg-admin select{width:100%;padding:10px 13px;border:2px solid #ddd;border-radius:var(--r2);font-size:.9rem;outline:none;transition:.2s}
.fg-admin input:focus,.fg-admin textarea:focus{border-color:var(--verde)}

/* BUTTONS */
.btn-primary{padding:11px 22px;background:var(--verde);color:#fff;border-radius:var(--r2);font-weight:800;font-size:.88rem;display:inline-flex;align-items:center;gap:7px;transition:.2s}
.btn-primary:hover{background:#006828}
.btn-danger{padding:7px 14px;background:#e74c3c;color:#fff;border-radius:var(--r2);font-weight:700;font-size:.8rem;transition:.2s}
.btn-danger:hover{background:#c0392b}
.btn-secondary{padding:11px 22px;background:var(--azul);color:#fff;border-radius:var(--r2);font-weight:800;font-size:.88rem;display:inline-flex;align-items:center;gap:7px;transition:.2s}

/* UPLOAD ZONE */
.upload-zone{border:2px dashed #ddd;border-radius:var(--r2);padding:28px;text-align:center;cursor:pointer;transition:.2s;background:#fafafa}
.upload-zone:hover,.upload-zone.over{border-color:var(--verde);background:rgba(0,156,59,.04)}
.upload-zone .icon{font-size:2.2rem;margin-bottom:6px}
.upload-zone p{color:#888;font-size:.85rem}
.upload-zone input[type=file]{display:none}
.prog-wrap{display:none;margin-top:10px}
.prog-bg{background:#eee;border-radius:99px;height:5px;overflow:hidden}
.prog-fill{height:100%;background:var(--verde);border-radius:99px;transition:width .3s;width:0%}
.prog-txt{font-size:.78rem;color:#888;margin-top:4px}

/* STATUS TOGGLE */
.status-row{display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:14px}
.btn-aberto,.btn-cerrado{padding:11px 26px;border-radius:var(--r2);font-weight:800;font-size:.9rem;border:2px solid;transition:.2s}
.btn-aberto{border-color:var(--verde);color:var(--verde)}
.btn-aberto.active,.btn-aberto:hover{background:var(--verde);color:#fff}
.btn-cerrado{border-color:#e74c3c;color:#e74c3c}
.btn-cerrado.active,.btn-cerrado:hover{background:#e74c3c;color:#fff}

/* TABLE */
.tbl{width:100%;border-collapse:collapse;font-size:.85rem}
.tbl th{background:var(--azul);color:#fff;padding:10px 13px;text-align:left;font-size:.78rem;text-transform:uppercase;letter-spacing:.5px}
.tbl td{padding:12px 13px;border-bottom:1px solid #f0f0f0;vertical-align:middle}
.tbl tr:hover td{background:#f9f9f9}
.tbl img{width:56px;height:56px;object-fit:cover;border-radius:var(--r2)}
.tag{display:inline-block;padding:3px 9px;border-radius:99px;font-size:.75rem;font-weight:700;background:rgba(0,156,59,.12);color:var(--verde)}

/* SLIDES LIST */
.slides-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:14px;margin-top:14px}
.slide-card{background:var(--bg);border-radius:var(--r2);overflow:hidden;box-shadow:0 2px 8px rgba(0,0,0,.07)}
.slide-card img{width:100%;height:120px;object-fit:cover}
.slide-card-body{padding:10px 12px 12px}
.slide-card-body p{font-size:.82rem;font-weight:700;margin-bottom:8px}

/* LOGO PREVIEW */
.logo-prev-wrap{display:flex;align-items:center;gap:18px;flex-wrap:wrap}
.logo-prev{width:110px;height:110px;border-radius:50%;object-fit:cover;border:3px solid var(--amarelo);display:none}
.logo-placeholder{width:110px;height:110px;border-radius:50%;background:var(--bg);border:2px dashed #ccc;display:flex;align-items:center;justify-content:center;font-size:2.5rem;color:#bbb}

/* TOAST */
#a-toasts{position:fixed;bottom:20px;right:16px;z-index:9999;display:flex;flex-direction:column;gap:8px}
.toast{background:var(--azul);color:#fff;padding:12px 18px;border-radius:var(--r2);font-weight:700;font-size:.88rem;border-left:4px solid var(--amarelo);box-shadow:0 4px 16px rgba(0,0,0,.2);animation:tin .3s ease,tout .3s 2.8s forwards}
.toast.ok{border-left-color:var(--verde)}
.toast.err{border-left-color:#e74c3c}
@keyframes tin{from{opacity:0;transform:translateX(60px)}to{opacity:1;transform:translateX(0)}}
@keyframes tout{to{opacity:0;transform:translateX(60px)}}

/* DASHBOARD CARDS */
.dash-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(180px,1fr));gap:18px;margin-bottom:22px}
.dash-card{background:#fff;border-radius:var(--r);padding:20px;box-shadow:var(--shadow);text-align:center}
.dash-card .icon{font-size:2.2rem;margin-bottom:6px}
.dash-card .num{font-size:1.8rem;font-weight:900;color:var(--azul)}
.dash-card .lbl{color:#888;font-size:.82rem}

@media(max-width:800px){
  .sidebar{width:200px}
  .form-row{grid-template-columns:1fr}
}
@media(max-width:600px){
  #admin-page{flex-direction:column}
  .sidebar{width:100%;height:auto;position:relative}
  .sb-nav ul{display:flex;flex-wrap:wrap}
  .sb-nav a{padding:10px 14px;font-size:.82rem}
  .admin-content{padding:16px}
}
